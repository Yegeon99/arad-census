# Arad Census 비주얼 업그레이드 지침서

이 문서는 arad-census 저장소 전용입니다. 저장소 루트에 넣고 Claude Code에 "이 문서를 읽고 Phase 1부터 시작해"라고 지시하면 됩니다.

목표: 7화면 페이지형 리포트를 스크롤을 내릴수록 이야기가 전개되는 "인터랙티브 리포트" 경험으로 만든다. Anime.js가 주인공이고 Motion은 보조로만 쓴다.

사용 라이브러리는 두 가지다.

| 라이브러리 | 역할 |
|---|---|
| Anime.js (`animejs`) | 스크롤 연동 연출, 순차 등장, 숫자 카운트업, 타임라인형 스토리텔링 |
| Motion (`motion`) | 보조 인터랙션. 내비게이션 호버, 코멘트 박스 등장 |

---

## 반드시 지킬 것 (공통 원칙)

1. **하나의 요소에 두 라이브러리 효과를 겹쳐 쓰지 않는다.** 같은 카드에 Anime.js 등장 연출과 Motion 등장 연출을 동시에 걸지 않는다.
2. **기존 3D 시각화 2곳은 내부를 수정하지 않는다.** 3D 캔버스 안에는 라이브러리를 적용하지 않고, 3D 섹션 바깥의 텍스트와 진입 연출에만 사용한다.
3. **애니메이션은 절제한다.** 화면당 핵심 연출 1~2개면 충분하다. 모든 요소가 움직이면 채용 담당자가 데이터에 집중할 수 없다.
4. **움직임 최소화 설정을 존중한다.** 사용자 시스템에 "동작 줄이기" 설정이 켜져 있으면 애니메이션을 생략하거나 즉시 완료 상태로 보여준다. (Anime.js는 조건 분기로 직접 처리, Motion은 자동 지원)
5. **성능 기준: 배포 후 모바일에서도 스크롤이 끊기지 않아야 한다.** 끊기면 해당 연출을 단순화하거나 제거한다.
6. **디자인 톤 유지.** 기존의 다크 톤과 Pretendard 타이포그래피(제목 ExtraBold, 소제목 SemiBold, 본문 Regular)를 그대로 따르고, 라이브러리 기본 색상이나 기본 폰트가 화면에 노출되지 않게 한다.
7. **문구 규칙 유지.** 화면에 새로 추가되는 모든 문구는 쉬운 용어로 쓰고, 줄표(—, –)와 하이픈 연결을 쓰지 않는다. 기존 리포트 문구는 이미 이 규칙으로 재작성되어 있으므로 수정하지 않는다.

## 설치

```bash
# Anime.js (v4)
npm install animejs

# Motion (구 Framer Motion)
npm install motion
```

참고용 기본 코드:

```ts
// Anime.js v4: 숫자 카운트업
import { animate } from "animejs";

animate(counterObj, {
  value: 31523,
  duration: 1600,
  ease: "outExpo",
  onUpdate: () => setDisplay(Math.round(counterObj.value)),
});
```

```tsx
// Motion: 화면에 들어올 때 박스 등장
import { motion } from "motion/react";

<motion.div
  initial={{ opacity: 0, y: 24 }}
  whileInView={{ opacity: 1, y: 0 }}
  viewport={{ once: true, amount: 0.3 }}
  transition={{ duration: 0.5, ease: "easeOut" }}
>
  {/* 박스 내용 */}
</motion.div>
```

Anime.js v4는 이전 버전과 사용법이 다르므로, 작업 전에 공식 문서(animejs.com)에서 v4 기준 스크롤 연동 방법을 확인하고 진행한다.

---

## Phase 1. Anime.js 스크롤 스토리텔링

1. 7개 화면 각각에 진입 연출을 설계한다. 화면마다 제목이 먼저 나타나고, 이어서 핵심 수치, 그다음 본문이 순서대로 등장하는 리듬을 통일한다.
2. 표본 수(31,523명) 같은 대표 숫자에는 화면 진입 시 카운트업을 적용한다. 각 숫자는 최초 1회만 실행한다.
3. 분포 차트(직업 분포, 명성 분포)는 화면에 들어올 때 막대나 선이 0에서 실제 값까지 차오르는 연출을 넣는다. 항목이 많은 차트는 전체를 순차 등장시키면 지루하므로 전체가 0.8초 이내에 완료되도록 한다.
4. 화면과 화면 사이 전환부에 짧은 연결 연출(구분선이 그려지거나 배경 요소가 이동하는 정도)을 1가지만 넣는다.
5. 3D 시각화 2곳은 내부를 수정하지 않는다. 대신 3D 섹션에 진입할 때 주변 텍스트가 어두워지며 3D에 시선이 모이는 연출만 추가한다.

## Phase 2. Motion 보조 인터랙션

1. 페이지 내비게이션(화면 이동 버튼이나 목차)에 호버와 현재 위치 표시 전환 효과를 넣는다.
2. 리포트 내 인용 카드나 해석 코멘트 박스에 등장 효과를 넣는다. Anime.js 연출과 같은 화면에서 겹치지 않는 요소에만 적용한다.

## Phase 3. 검수 게이트

아래 항목을 모두 확인한 후 스크린샷과 함께 보고한다.

- [ ] 7개 화면 전부에서 스크롤 연출이 의도한 시점에 정확히 발동한다
- [ ] 빠르게 스크롤해도 연출이 밀리거나 중복 실행되지 않는다
- [ ] 3D 시각화 2곳의 프레임이 기존과 동일하게 유지된다
- [ ] 모바일에서 스크롤이 끊기지 않는다 (끊기면 해당 연출 단순화)
- [ ] 기존 태그(v2.0-2026-08) 대비 새 태그로 버전을 올려 배포한다

---

## 실행 방식

1. Phase 단위로 진행하고, 한 Phase가 끝나면 반드시 배포 미리보기 링크나 스크린샷으로 검수받은 뒤 다음 Phase로 넘어간다.
2. 오류가 나면 스스로 해결하되, 라이브러리 문서 확인이 필요한 경우 추측하지 말고 공식 문서를 조회해서 해결한다.
3. 작업 완료 후에는 무엇이 바뀌었는지 화면 기준으로 한 줄씩 요약해서 보고한다. (예: "1화면 제목이 스크롤 진입 시 순서대로 나타납니다")

## 넣지 말아야 할 것 (금지 목록)

- 자동 재생되는 무한 반복 애니메이션 (로딩 표시 제외)
- 마우스를 따라다니는 커서 효과
- 페이지 전체를 덮는 화면 전환 연출
- 소리
- 3초 이상 걸리는 인트로
- 데이터 수치를 가리거나 늦게 보여주는 연출
